{
  "type": "module",
  "dependencies": {
    "@composio/core": "^0.6.2"
  },
  "devDependencies": {
    "nock": "^14.0.10",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  }
}
